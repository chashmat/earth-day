window.addEventListener("load", () => {
    let loaderBg = document.getElementsByClassName("loader")[0];
    let loader = document.getElementsByClassName("loader-mov")[0];
    loader.classList.add("mov");
    loaderBg.classList.add("bg");
});